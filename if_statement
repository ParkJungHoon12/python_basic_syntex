# if 문의 기본문법
# else는 optional한 요소
# else 상단에 있는 if 또는 elif에 종속된다
# elif도 마찬가지로 elif상단에 if에 종속된다

# 삼항연산자
# 한줄로 다 옮긴다
# 먼저 if문의 실행문을 if앞으로 옮긴다.
# 세미콜론을 다 뺀다.
# a = 10
# print('a is up then 10') if a > 10 else print('a is down then 10')

# 실행문을 실행하기 위해 사용한다기 보다는, 
# 참인경우에 어떤값을, 거짓인경우에 어떤 값을 쉽게 담기 위해

'''
def hungry(money, hun):
    if money >= 10000 and hun == 1:
        return "\n저녁을 사먹으시오"
    
    return "\n1집에 가시오" 



money = int(input("돈 얼마 잇니? "))
hun = int(input("배가 고프니?\n1. 배고프다\n2. 아니다\n숫자를 선택하시오 : "))

print(hungry(money, hun)) '''


# 로또 번호 생성기
# 랜덤의 값을 추출하는 파이썬 라이브러리 -> random()

#import random

#lista = []

#for i in range(0,6):
#  lista.append(random.randint(1,45))

#print(lista)


v1 = list(range(1,10))

print(v1)


